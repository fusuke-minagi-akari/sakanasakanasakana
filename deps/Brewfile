# Brewfile — macOS dependencies. Install: brew bundle --file deps/Brewfile
# See DEPENDENCIES.md for what each maps to. herdr is NOT here (install from herdr.dev).

# --- runtime baseline + daemon ---
brew "git"
brew "jq"
brew "gh"           # branch-labels PR number; nippo/standup/research

# --- show: in-terminal viewers ---
brew "chafa"        # images
brew "mpv"          # video / audio
brew "glow"         # markdown

# --- Claude Code: diagrams / pdf / tables ---
brew "node"         # npx @mermaid-js/mermaid-cli, md-to-pdf
brew "python@3.12"  # render_table.py, skills (macOS also ships python3)

# --- optional (enable with bootstrap.sh --full) ---
# brew "uv"                    # melchior_headless
# brew "go-task"               # `task` for cpolaris venv setup
# cask "kitty"                 # outer terminal for inline media (show)
# cask "betterdisplay"         # melchior pseudo-headless

# NOTE: matplotlib is a pip dep (deps/requirements.txt), not brew.
# NOTE: CJK font for render_table.py — macOS ships Hiragino; nothing to install.
